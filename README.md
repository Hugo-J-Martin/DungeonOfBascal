Movement Controls: WASD
Fire Button: Mouse Button 1

Need to collect all floating heads while surviving/killing enemy skeletons


Source files are in Master branch, not main. Main only has finished build and git attributes and ignore
